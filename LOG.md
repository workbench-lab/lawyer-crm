# Лог работы над тестовым заданием

## Кратко (TL;DR)

**Что делал сам, а что AI:** Сам — создал и настроил внешние сервисы (Supabase, Telegram-бот, GitHub, Vercel), выполнил SQL-схему, принимал продуктовые и дизайн-решения, тестировал прототип на живом URL и находил баги вёрстки на реальных кейсах. AI (Claude Code) — по моим требованиям написал весь код, стили и документацию, вёл деплой и прогонял проверки после каждого шага (валидность ключей, RLS-политики, отсутствие токена в клиентском бандле).

**Почему такой стек:** Vite + React + TypeScript + Supabase + Vercel — реальный Postgres без своего бэкенда, статика и serverless-функции в одном бесплатном деплое; автофоллбэк на localStorage, чтобы демо работало, даже если free-tier база уснёт.

**Время:** 9 июля 2026, 17:37 — 18:28 (основной функционал задеплоен за ~50 минут), затем до ~19:00 — итерация по UX/UI (экран входа, кастомные компоненты, маска телефона, адаптив).

**Бонус:** при добавлении клиента юристу автоматически приходит Telegram-уведомление — serverless-функция на Vercel (токен бота только в серверных env), в сообщении имя, телефон (тап — копирует), время по МСК и ссылка на CRM.

---

## Что делал человек

- Создал проект в Supabase и Telegram-бота через @BotFather, передал ключи.
- Выполнил SQL-схему в Supabase SQL Editor.
- Аккаунты GitHub/Vercel, подтверждение деплоя, снятие Deployment Protection.
- Приёмочное тестирование на живом URL (добавление клиента, статусы, счётчики, Telegram-сообщение), поиск багов вёрстки на edge-кейсах (длинные имена, мобильные экраны) и постановка задач на редизайн.

## Что делал AI (Claude Code)

- Спланировал архитектуру (репозиторий данных с фоллбэком, serverless-уведомления) и задал уточняющие вопросы до старта.
- Написал весь код: скаффолд, слой данных Supabase/localStorage, UI-компоненты, стили, Telegram-функцию, смоук-тесты.
- README, SQL-схему, деплой через gh/vercel CLI, проверки на каждом шаге (валидность токена, наличие таблицы, сборка, отсутствие секретов в бандле).

## Почему такой стек

- **Vite + React + TS** — самый быстрый способ собрать типобезопасный UI без лишнего.
- **Supabase** — настоящий Postgres без своего бэкенда, названный в задании как рекомендуемый вариант.
- **Фоллбэк на localStorage** — живое демо не сломается у проверяющего, даже если free-tier база уснёт.
- **Vercel** — статика + serverless-функция в одном бесплатном деплое; токен бота остаётся на сервере, а не в бандле.
- **Дизайн без UI-библиотек** — рукописный CSS с токенами, Inter + Playfair Display, иконки lucide-react: полный контроль и минимальный бандл.

## Хронология

- 17:37 — старт, уточнение требований (хранилище, уведомления, деплой), план.
- 17:50 — скаффолд Vite + React + TS, git.
- 18:05 — приложение целиком (слой данных с фоллбэком, компоненты, стили), Telegram-функция, смоук-тест прошёл (реальное сообщение доставлено).
- 18:15 — GitHub + Vercel: деплой, снятие Deployment Protection, SQL в Supabase.
- 18:28 — финальная приёмка на живом URL: CRUD через anon-ключ, Telegram из прода, отсутствие токена в бандле.
- 18:35 → ~19:00 — итерация 2 (UX/UI): экран входа с демо-авторизацией, кастомные дропдауны/модалки/тосты, маска телефона +7, анимации, мобильные карточки вместо таблицы, новый формат Telegram-уведомления; фиксы адаптива по скриншотам с реальных устройств.
